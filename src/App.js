import React, {useState} from 'react';
import './App.css';

function App() {
  const stages = [
    'Select Date and Tickets',
    'Pay',
    'Success'
  ];

  const [stageIndex, setStageIndex] = useState(0);

  return (
    <div className="App">
  
    </div>
  );
}

export default App;
